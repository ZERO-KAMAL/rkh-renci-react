const SESSION_TIMEOUT_MSG = 'Session Timeout! Please login again'
const SESSION_FAILED_RENEW = 'Please login again!'
const PERMISSION_DENIED = 'No permissions enabled for this user role '

const Auth = {
  SESSION_TIMEOUT_MSG,
  SESSION_FAILED_RENEW,
  PERMISSION_DENIED,
}

export default Auth
